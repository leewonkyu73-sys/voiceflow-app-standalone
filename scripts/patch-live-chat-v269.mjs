import fs from 'node:fs/promises';

const file=new URL('../public/app.js',import.meta.url);
let s=await fs.readFile(file,'utf8');
const must=(re,repl,label)=>{if(!re.test(s))throw new Error(`patch_missing:${label}`);s=s.replace(re,repl)};

must(/function captionCard\(c\)\{.*?\}\nfunction room/s,
`function captionCard(c){const source=c.display_source_language||c.detected_language||c.language||'ko-KR';const target=localStorage.targetLanguage||c.display_target_language||locale[state.lang]||'en-US';const light=c.validation?.light||'yellow',score=c.validation?.score??0;const mine=(c.peer_id&&state.meeting?._peer&&c.peer_id===state.meeting._peer)||c.speaker===(localStorage.displayName||state.user?.name);const translated=String(c.translation||c.translations?.[target]||'').trim(),original=String(c.text||'').trim();const changed=translated&&translated!==original;const feedback=c.translation_feedback?.[target];const incorrect=feedback?.status==='incorrect';const status=incorrect?'잘못 번역됨':changed?(light==='green'?'확인':light==='yellow'?'검토 권장':'검토 필요'):'번역 대기';const actions=\`<div class="chat-message-actions">\${mine?\`<button type="button" data-caption-delete="\${esc(c.id||'')}" aria-label="원문 삭제">삭제</button>\`:''}\${changed&&!incorrect?\`<button type="button" data-translation-wrong="\${esc(c.id||'')}" data-target="\${esc(target)}">잘못 번역됨</button>\`:''}\${incorrect?'<span class="translation-flag">⚠ 잘못 번역됨</span>':''}</div>\`;return \`<article class="chat-msg \${mine?'mine':'other'}" data-caption-id="\${esc(c.id||'')}"><div class="chat-meta"><b>\${esc(c.speaker||'Participant')}</b><span>\${langLabel(source)} · \${new Date(c.created_at||Date.now()).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}</span></div><div class="chat-bubble"><p class="chat-original">\${esc(original)}</p><div class="chat-translation \${changed?'':'waiting'} \${incorrect?'incorrect':''}"><small>\${langLabel(target)} 번역</small><p>\${esc(changed?translated:'번역 처리 중…')}</p></div></div><div class="chat-assurance \${incorrect?'red':changed?light:'yellow'}">\${changed&&!incorrect?'번역 품질 ':''}\${changed&&!incorrect?score+'% · ':''}\${status}</div>\${actions}</article>\`}
function room`,'chat-bubble-card');

must(/function room\(\)\{.*?\}\nfunction resultReview/s,
`function room(){const m=state.meeting;if(!m)return home();const syncing=m._creating?'서버 연결 중':m._createError?'서버 연결 확인 필요':'연결됨';const capture=state.media.videoRecording?'영상 녹화 중':state.media.recording?'음성 녹음 중':'준비 중';return shell(\`<section class="room-head simple-room-head"><div><small>LIVE TRANSLATION CHAT</small><h1>\${esc(m.title||'Meeting')}</h1><small id="meetingSyncState">\${syncing} · 실시간 텍스트/동시번역</small></div><div class="live-badge"><span></span>\${capture}</div></section><section class="chat-live-shell"><section class="chat-toolbar"><div class="language-strip"><label>말하는 언어<select id="liveSourceLang"><option value="ko-KR">한국어</option><option value="vi-VN">Tiếng Việt</option><option value="en-US">English</option><option value="zh-CN">中文</option></select></label><span>→</span><label>내 화면 번역<select id="liveTargetLang"><option value="vi-VN">Tiếng Việt</option><option value="ko-KR">한국어</option><option value="en-US">English</option><option value="zh-CN">中文</option></select><small class="personal-language-note">상대방과 별개로 내 화면에만 적용</small></label></div><div class="chat-status-row"><span class="live-dot">● LIVE</span><span id="speechState">\${state.media.recording?'듣는 중':'대기'}</span><span>메시지 \${state.captions.length}</span></div></section><section id="videoPreviewPanel" class="video-preview-panel \${state.media.videoRecording?'show':''}"><video id="localVideoPreview" autoplay muted playsinline></video></section><section id="liveChatStream" class="live-chat-stream" aria-live="polite">\${state.captions.slice(-80).map(captionCard).join('')||'<div class="empty premium-empty"><b>말을 시작하세요</b><span>말하는 동안 텍스트가 나타나고 확정되면 번역이 같은 말풍선에 표시됩니다.</span></div>'}<article id="interimBubble" class="chat-msg mine interim \${state.interimText?'show':''}"><div class="chat-meta"><b>\${esc(localStorage.displayName||state.user?.name||'나')}</b><span>인식 중</span></div><div class="chat-bubble"><p id="interimText">\${esc(state.interimText||'')}</p><div class="chat-translation waiting"><small>번역</small><p>문장 확정 후 번역됩니다…</p></div></div></article></section><section id="liveComposer" class="chat-compose-row"><div class="record-mini"><span class="record-orb \${state.media.recording||state.media.videoRecording?'active':''}"><i></i></span><div><small>REC</small><b id="recordTime">\${fmt(state.media.elapsed)}</b></div></div><input id="chatText" placeholder="직접 입력하거나 말하세요"><button id="sendChat">전송</button><button id="stopCapture" class="danger">■ 종료</button></section><section id="liveTestRow" class="chat-test-row"><button id="openAiSelfTest">AI 직원 자동대화 테스트</button><button id="micCheck">마이크 상태 확인</button></section></section>\`)}
function resultReview`,'live-chat-room');

must(/function startSpeech\(\)\{.*?\}\nasync function postCaption/s,
`function updateInterimText(text=''){state.interimText=text;const b=document.querySelector('#interimBubble'),e=document.querySelector('#interimText'),st=document.querySelector('#speechState');if(b)b.classList.toggle('show',!!text);if(e)e.textContent=text;if(st){const fallback=state.media.stt==='unsupported'?'현재 브라우저는 실시간 음성 텍스트 변환을 지원하지 않습니다':state.media.stt==='denied'?'음성인식 권한을 확인하세요':state.media.stt==='error'?'음성인식 연결을 확인하세요':state.media.recording?'듣는 중':'대기';st.textContent=text?'말하는 중…':fallback}if(text){const stream=document.querySelector('#liveChatStream');if(stream)stream.scrollTop=stream.scrollHeight}}

function captureCaptionEditorState(){const active=document.activeElement;for(const kind of ['original','translation']){const panel=document.querySelector(kind==='original'?'[data-original-panel]:not([hidden])':'[data-translation-panel]:not([hidden])');if(!panel)continue;const input=panel.querySelector('textarea'),id=kind==='original'?panel.dataset.originalPanel:panel.dataset.translationPanel;return {kind,id,value:input?.value||'',focused:active===input,start:input?.selectionStart??0,end:input?.selectionEnd??0}}return null}
function restoreCaptionEditorState(edit){if(!edit?.id)return;const panel=[...document.querySelectorAll(edit.kind==='original'?'[data-original-panel]':'[data-translation-panel]')].find(x=>(edit.kind==='original'?x.dataset.originalPanel:x.dataset.translationPanel)===edit.id),button=[...document.querySelectorAll(edit.kind==='original'?'[data-original-edit]':'[data-translation-edit]')].find(x=>(edit.kind==='original'?x.dataset.originalEdit:x.dataset.translationEdit)===edit.id);if(!panel)return;panel.hidden=false;if(button)button.hidden=true;const input=panel.querySelector('textarea');if(input){input.value=edit.value;if(edit.focused){input.focus({preventScroll:true});try{input.setSelectionRange(edit.start,edit.end)}catch{}}}}
function renderRoomStable(forceBottom=false){const current=document.querySelector('#liveChatStream'),oldTop=current?.scrollTop||0,nearBottom=!current||current.scrollHeight-current.scrollTop-current.clientHeight<90,edit=captureCaptionEditorState();render();restoreCaptionEditorState(edit);const next=document.querySelector('#liveChatStream');if(next)next.scrollTop=(forceBottom||nearBottom)?next.scrollHeight:oldTop}

function stopServerSpeechFallback(){clearTimeout(state._serverSttTimer);state._serverSttTimer=null;try{if(state._serverSttRecorder&&state._serverSttRecorder.state!=='inactive')state._serverSttRecorder.stop()}catch{}state._serverSttRecorder=null;state._serverSttActive=false}
function startServerSpeechFallback(){if(state._serverSttActive||!state.media.stream||!window.MediaRecorder||!state.meeting)return;state._serverSttActive=true;const cycle=()=>{if(!state.meeting||!state.media.recording){stopServerSpeechFallback();return}if(state.media.paused){state._serverSttTimer=setTimeout(cycle,700);return}let chunks=[];let recorder;try{const preferred=['audio/webm;codecs=opus','audio/webm','audio/ogg;codecs=opus'].find(x=>MediaRecorder.isTypeSupported?.(x));recorder=new MediaRecorder(state.media.stream,preferred?{mimeType:preferred}:undefined)}catch{state._serverSttActive=false;state.media.sttFallback='unsupported';return}state._serverSttRecorder=recorder;recorder.ondataavailable=e=>{if(e.data?.size)chunks.push(e.data)};recorder.onstop=async()=>{const blob=new Blob(chunks,{type:recorder.mimeType||'audio/webm'});chunks=[];if(blob.size>900&&!state.media.paused&&state.meeting){try{state.media.sttFallback='uploading';const lang=localStorage.sourceLanguage||state.meeting?.language||locale[state.lang]||'ko-KR';const response=await fetch('/api/v1/meetings/'+encodeURIComponent(state.meeting.id)+'/transcribe',{method:'POST',headers:{'content-type':blob.type||'audio/webm','x-voice-language':lang},body:blob});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||('HTTP '+response.status));const text=String(data.text||'').trim();if(text){state.media.stt='server';state.media.sttFallback='ok';state._lastServerSpeechAt=Date.now();updateInterimText('');await postCaption(text,'server')}}catch(e){state.media.sttFallback='error';state.media.sttError=String(e?.message||e)}}if(state.meeting&&state.media.recording){state._serverSttTimer=setTimeout(cycle,250)}else stopServerSpeechFallback()};try{recorder.start();state._serverSttTimer=setTimeout(()=>{try{if(recorder.state!=='inactive')recorder.stop()}catch{}},6500)}catch{state._serverSttActive=false}};cycle()}
function startSpeech(){startServerSpeechFallback();const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!state.meeting)return;if(!SR){state.media.stt='unsupported';state.media.sttError='speech-recognition-unavailable';state._speechStarting=false;return}if(state._speechStarting)return;try{state._speech?.abort?.()}catch{}const generation=(state._speechGeneration||0)+1;state._speechGeneration=generation;const r=new SR();state._speech=r;state._speechStarting=true;r.continuous=true;r.interimResults=true;r.maxAlternatives=1;r.lang=localStorage.sourceLanguage||state.meeting?.language||locale[state.lang];r.onstart=()=>{if(generation!==state._speechGeneration)return;state._speechStarting=false;state.media.stt='listening';state.media.sttError='';updateInterimText('')};r.onresult=async e=>{if(generation!==state._speechGeneration)return;let interim='';for(let i=e.resultIndex;i<e.results.length;i++){const text=e.results[i][0].transcript.trim();if(!text)continue;if(e.results[i].isFinal){state._lastBrowserPreviewAt=Date.now();state.media.stt='speaking';interim+=(interim?' ':'')+text}else{state._lastBrowserInterimAt=Date.now();state.media.stt='speaking';interim+=(interim?' ':'')+text}}if(interim)updateInterimText(interim)};r.onerror=e=>{if(generation!==state._speechGeneration)return;state._speechStarting=false;const code=e.error||'error';state.media.sttError=code;state.media.stt=(code==='not-allowed'||code==='service-not-allowed')?'denied':code==='no-speech'?'listening':'error';updateInterimText('')};r.onend=()=>{if(generation!==state._speechGeneration)return;state._speechStarting=false;if(!state.media.recording)updateInterimText('');if(state.meeting&&state.media.recording&&!state.media.paused&&!['unsupported','denied'].includes(state.media.stt))setTimeout(startSpeech,state.media.stt==='error'?1200:350)};try{r.start()}catch(e){state._speechStarting=false;state.media.stt='error';state.media.sttError=String(e?.message||e);updateInterimText('')}}
async function postCaption`,'interim-speech');

must(/async function postCaption\(text\)\{.*?\}\nfunction pollCaptions/s,
`function speechMatchesFixedLanguage(text,language){const value=String(text||''),hangul=/[가-힣]/.test(value),kana=/[ぁ-んァ-ン]/.test(value),han=/[\\u4e00-\\u9fff]/.test(value);if(language==='ko-KR')return !kana&&(!han||hangul);if(language==='zh-CN')return !hangul&&!kana;if(language==='vi-VN'||language==='en-US')return !hangul&&!kana&&!han;return true}
async function postCaption(text,origin='manual'){if(!state.meeting||!text.trim())return;const clean=text.trim(),speechOrigin=origin==='browser'||origin==='server',fixedLanguage=localStorage.sourceLanguage||state.meeting?.language||locale[state.lang]||'ko-KR',speechKey=clean.toLocaleLowerCase().replace(/[\\s.,!?'"’“”]+/g,''),lastSpeech=state._lastSpeechCommit;if(speechOrigin&&!speechMatchesFixedLanguage(clean,fixedLanguage)){state.media.stt='listening';state.media.sttError='language-mismatch';const status=document.querySelector('#speechState');if(status)status.textContent=langLabel(fixedLanguage)+'가 아닌 인식 결과 제외';return null}if(speechOrigin&&lastSpeech&&lastSpeech.key===speechKey&&Date.now()-lastSpeech.at<12000)return lastSpeech.row||null;if(speechOrigin)state._lastSpeechCommit={key:speechKey,at:Date.now(),row:null};const target=localStorage.targetLanguage||'en-US',detected=speechOrigin?fixedLanguage:(window.VoiceFlowLanguage?.detect?.(clean)||fixedLanguage);const tempId='local_'+Date.now()+'_'+Math.random().toString(36).slice(2,7),temp={id:tempId,meeting_id:state.meeting.id,peer_id:state.meeting._peer,speaker:localStorage.displayName||state.user?.name||'Participant',language:detected,detected_language:detected,text:clean,translation:'',validation:null,created_at:Date.now(),pending:true};state.captions.push(temp);try{const response=await api(\`/api/v1/meetings/\${state.meeting.id}/captions\`,{method:'POST',body:JSON.stringify({peer_id:state.meeting._peer,speaker:localStorage.displayName||state.user?.name||'Participant',language:detected,detected_language:detected,target_language:target,text:clean,input_mode:speechOrigin?'speech':'manual',final:true})});const row=response.data||response,mapped={...row,display_target_language:target,translation:row.translation||row.translations?.[target]||'',validation:row.validation||row.assurance?.[target]||null,pending:false};if(speechOrigin&&state._lastSpeechCommit?.key===speechKey)state._lastSpeechCommit.row=mapped;const i=state.captions.findIndex(x=>x.id===tempId);if(i>=0)state.captions[i]=mapped;else if(!state.captions.some(x=>x.id===mapped.id))state.captions.push(mapped);if(row.created_at)state.captionSince=Math.max(state.captionSince,Number(row.created_at)||0);if(state.view==='room')renderRoomStable(true);return mapped}catch(e){const i=state.captions.findIndex(x=>x.id===tempId);if(i>=0)state.captions[i]={...state.captions[i],pending:false,send_error:String(e?.message||e)};if(state.view==='room')renderRoomStable(false);throw e}}
function pollCaptions`,'optimistic-caption-send');

must(/function pollCaptions\(\)\{.*?\}\nfunction pollSignals/s,
`function pollCaptions(){clearInterval(state.poll);const tick=async()=>{if(!state.meeting)return;try{const target=localStorage.targetLanguage||'en-US';const r=await api(\`/api/v1/meetings/\${state.meeting.id}/captions?target=\${encodeURIComponent(target)}&since=\${state.captionSince}\`);let changed=false;for(const c of r.data||[]){state.captionSince=Math.max(state.captionSince,c.created_at||0);const i=state.captions.findIndex(x=>x.id===c.id),mapped={...c,display_target_language:target};if(i>=0){const next={...state.captions[i],...mapped};if(JSON.stringify(next)!==JSON.stringify(state.captions[i])){state.captions[i]=next;changed=true}}else{state.captions.push(mapped);changed=true}}if(changed&&state.view==='room')renderRoomStable(false)}catch(e){const st=document.querySelector('#speechState');if(st)st.textContent='번역 연결 확인 필요'}};void tick();state.poll=setInterval(tick,1100)}
function pollSignals`,'target-aware-caption-poll');

s=s.replace(/const targetLangV263=\$\('#liveTargetLang'\);if\(targetLangV263\)\{targetLangV263\.value=localStorage\.language\|\|'vi-VN';targetLangV263\.onchange=\(\)=>\{localStorage\.language=targetLangV263\.value;render\(\)\}\}/,
`const targetLangV263=$('#liveTargetLang');if(targetLangV263){targetLangV263.value=localStorage.targetLanguage||'en-US';targetLangV263.onchange=()=>{localStorage.targetLanguage=targetLangV263.value;state.captionSince=0;state.captions=[];render();pollCaptions()}}`);

if(!s.includes("location.href='/ai-meeting-lab.html?mode=translation-selftest'")){
  must(/function bind\(\)\{\n/,
  `function bind(){\n  $('#openAiSelfTest')?.addEventListener('click',()=>{location.href='/ai-meeting-lab.html?mode=translation-selftest'});\n`,'ai-selftest-bind-anchor');
}

must(/function bind\(\)\{\n/,
`function bind(){\n  const chatBox=$('#chatText');if(chatBox){const grow=()=>{chatBox.style.height='auto';chatBox.style.height=Math.min(chatBox.scrollHeight,180)+'px'};chatBox.addEventListener('input',grow);chatBox.addEventListener('keydown',e=>{if(e.key==='Enter'&&(e.ctrlKey||e.metaKey)){e.preventDefault();$('#sendChat')?.click()}});grow()}\n  document.querySelectorAll('.chat-compose-row').forEach((el,i)=>{if(i>0)el.remove()});\n  document.querySelectorAll('.chat-test-row').forEach((el,i)=>{if(i>0)el.remove()});\n  document.querySelectorAll('[data-caption-delete]').forEach(btn=>btn.onclick=async()=>{const id=btn.dataset.captionDelete;if(!id||!state.meeting?._peer)return;if(!confirm('이 원문을 삭제할까요? 삭제하면 상대방 화면에서도 사라집니다.'))return;try{await api(\`/api/v1/meetings/\${state.meeting.id}/captions/\${id}\`,{method:'DELETE',body:JSON.stringify({peer_id:state.meeting._peer})});state.captions=state.captions.filter(c=>c.id!==id);render()}catch(e){alert('삭제 실패: '+e.message)}});\n  document.querySelectorAll('[data-translation-wrong]').forEach(btn=>btn.onclick=async()=>{const id=btn.dataset.translationWrong,target=btn.dataset.target||localStorage.targetLanguage||'en-US';if(!id||!state.meeting)return;try{const r=await api(\`/api/v1/meetings/\${state.meeting.id}/captions/\${id}/translation-feedback\`,{method:'POST',body:JSON.stringify({peer_id:state.meeting._peer,target,reason:'wrong_translation'})});const c=state.captions.find(x=>x.id===id);if(c)c.translation_feedback={...(c.translation_feedback||{}),[target]:r.data?.feedback||{status:'incorrect'}};render()}catch(e){alert('번역 표시 실패: '+e.message)}});\n`,'live-room-actions');


s=s.replace("clearInterval(state.signalPoll);try{state._speech?.stop();","clearInterval(state.signalPoll);stopServerSpeechFallback();try{state._speech?.stop();");

await fs.writeFile(file,s,'utf8');
const out=await fs.readFile(file,'utf8');
new Function(out);
if((out.match(/class="chat-compose-row"/g)||[]).length!==1)throw new Error('live_composer_source_count_not_one');
if(!out.includes("localStorage.targetLanguage||'en-US'"))throw new Error('target_language_state_missing');
if(!out.includes('data-caption-delete'))throw new Error('caption_delete_ui_missing');
if(!out.includes('data-translation-wrong'))throw new Error('translation_feedback_ui_missing');
if(!out.includes('startServerSpeechFallback'))throw new Error('server_stt_fallback_missing');
if(!out.includes("/transcribe"))throw new Error('server_stt_endpoint_binding_missing');
if(!out.includes('target_language:target'))throw new Error('caption_target_request_missing');
if(!out.includes("c.translations?.[target]"))throw new Error('caption_translation_map_missing');
if(!out.includes("tempId='local_'"))throw new Error('optimistic_caption_missing');
if(!out.includes('state._lastBrowserInterimAt=Date.now()')||!out.includes('state._lastBrowserPreviewAt=Date.now()'))throw new Error('browser_preview_guard_missing');
if(!out.includes('function renderRoomStable(forceBottom=false)')||out.includes("render();setTimeout(()=>{const stream=document.querySelector('#liveChatStream')"))throw new Error('stable_chat_render_missing');
if(out.includes("state.captions.push(temp);if(state.view==='room')renderRoomStable(true)"))throw new Error('optimistic_caption_double_render_present');
if(!out.includes("if(changed&&state.view==='room')renderRoomStable(false)"))throw new Error('caption_poll_change_guard_missing');
if(out.includes("postCaption(text,'browser')")||!out.includes("postCaption(text,'server')")||!out.includes("lastSpeech.key===speechKey"))throw new Error('server_authoritative_speech_missing');
if(out.includes('browserFinalArrived=')||out.includes('const browserFresh='))throw new Error('browser_suppression_still_present');
if(!out.includes("'x-voice-language':lang")||out.includes("'x-voice-language':'auto'"))throw new Error('fixed_server_language_missing');
console.log('VoiceFlow chat correction + personal target language v2.7.7 applied');

if(!out.includes('captureCaptionEditorState')||!out.includes('restoreCaptionEditorState')||!out.includes('setSelectionRange'))throw new Error('caption_editor_state_guard_missing');
if(!out.includes('speechMatchesFixedLanguage')||!out.includes("speechOrigin?fixedLanguage")||!out.includes("'x-voice-language':lang")||!out.includes('language-mismatch'))throw new Error('fixed_speech_language_guard_missing');
