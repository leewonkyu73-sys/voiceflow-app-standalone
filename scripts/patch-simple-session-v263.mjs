import './patch-app-v26.mjs';
import fs from 'node:fs/promises';

const file=new URL('../public/app.js',import.meta.url);
let s=await fs.readFile(file,'utf8');
const must=(re,repl,label)=>{if(!re.test(s))throw new Error(`patch_missing:${label}`);s=s.replace(re,repl)};

must(/function home\(\)\{.*?\}\nfunction work/s,
`function home(){return shell(\`\${ongoing()}<section class="hero premium-hero simple-home"><div class="eyebrow">STAR45 AI MEETING</div><h1>바로 말하고 기록하세요</h1><p>녹음·녹화와 동시에 텍스트 변환·통역·회의/상담 노트를 한 화면에서 처리합니다.</p></section><section class="quick-capture-grid"><button id="quickAudioStart" class="capture-card primary-capture"><span class="capture-icon">●</span><div><small>VOICE</small><h2>음성 녹음 시작</h2><p>마이크 → 실시간 텍스트 → 통역</p></div></button><button id="quickVideoStart" class="capture-card"><span class="capture-icon">▣</span><div><small>VIDEO</small><h2>영상 녹화 시작</h2><p>카메라+마이크 → 텍스트 → 통역</p></div></button></section><section class="simple-info"><span>1. 시작</span><span>2. 자동 텍스트·통역</span><span>3. 같은 화면 노트</span><span>4. 종료 후 회의결과</span></section><section class="panel compact-panel"><div class="panel-title"><div><small>OPTION</small><h2>필요할 때만</h2></div></div><div class="actions"><button data-nav="meeting">회의 제목·언어 먼저 설정</button><button data-href="/work-calendar.html">일정</button><button data-href="/board.html">자료</button>\${state.user?.role==='admin'?'<button data-href="/admin-integrations.html">API · 연동 설정</button>':''}</div></section>\`)}
function work`,'simple-home');

must(/function room\(\)\{.*?\}\nfunction resultReview/s,
`function room(){const m=state.meeting;if(!m)return home();const syncing=m._creating?'서버 연결 중':m._createError?'서버 연결 확인 필요':'연결됨';const capture=state.media.videoRecording?'영상 녹화 중':state.media.recording?'음성 녹음 중':'준비 중';return shell(\`<section class="room-head simple-room-head"><div><small>LIVE MEETING / CONSULTATION</small><h1>\${esc(m.title||'Meeting')}</h1><small id="meetingSyncState">\${syncing}</small></div><div class="live-badge"><span></span>\${capture}</div></section><section class="live-workspace"><div class="live-main"><section class="record-panel premium-rec simple-rec"><div class="record-orb \${state.media.recording||state.media.videoRecording?'active':''}"><span></span></div><div><small>LIVE CAPTURE</small><h2>\${capture}</h2><b id="recordTime">\${fmt(state.media.elapsed)}</b></div><button id="stopCapture" class="danger">■ 종료</button></section><section id="videoPreviewPanel" class="video-preview-panel \${state.media.videoRecording?'show':''}"><video id="localVideoPreview" autoplay muted playsinline></video></section><section class="language-strip"><label>내 언어<select id="liveSourceLang"><option value="ko-KR">한국어</option><option value="vi-VN">Tiếng Việt</option><option value="en-US">English</option><option value="zh-CN">中文</option></select></label><span>→</span><label>통역 표시<select id="liveTargetLang"><option value="vi-VN">Tiếng Việt</option><option value="ko-KR">한국어</option><option value="en-US">English</option><option value="zh-CN">中文</option></select></label></section><section class="panel transcript-panel"><div class="panel-title"><div><small>LIVE TEXT & INTERPRETATION</small><h2>실시간 텍스트 · 통역</h2></div><span>\${state.captions.length}</span></div>\${state.captions.slice(-20).map(captionCard).join('')||'<div class="empty premium-empty"><b>말을 시작하세요</b><span>음성이 텍스트로 변환되고 선택 언어로 통역됩니다.</span></div>'}</section></div><aside class="session-note-panel"><div><small>MEETING / CONSULTATION NOTE</small><h2>회의 · 상담 노트</h2><p>중요 내용, 고객 요청, 결정사항을 바로 적으세요.</p></div><textarea id="sessionNoteText" placeholder="예: 고객이 9월 10일까지 샘플 견적 요청"></textarea><button id="saveSessionNote" class="primary">노트 추가</button><div id="sessionNotes">\${(state.sessionNotes||[]).slice().reverse().map(n=>\`<article><small>\${esc(n.time)}</small><p>\${esc(n.text)}</p></article>\`).join('')||'<p class="empty">작성된 노트가 없습니다.</p>'}</div></aside></section>\`)}
function resultReview`,'simple-room');

must(/function bind\(\)\{\n/,
`function bind(){\n  const quickAudioV263=$('#quickAudioStart');if(quickAudioV263)quickAudioV263.onclick=()=>launchSimpleSession('audio');\n  const quickVideoV263=$('#quickVideoStart');if(quickVideoV263)quickVideoV263.onclick=()=>launchSimpleSession('video');\n  const stopV263=$('#stopCapture');if(stopV263)stopV263.onclick=endMeeting;\n  const noteV263=$('#saveSessionNote');if(noteV263)noteV263.onclick=saveSessionNote;\n  const targetLangV263=$('#liveTargetLang');if(targetLangV263){targetLangV263.value=localStorage.language||'vi-VN';targetLangV263.onchange=()=>{localStorage.language=targetLangV263.value;render()}}\n  const sourceLangV263=$('#liveSourceLang');if(sourceLangV263){sourceLangV263.value=state.meeting?.language||localStorage.sourceLanguage||'ko-KR';sourceLangV263.onchange=()=>{localStorage.sourceLanguage=sourceLangV263.value}}\n  const previewV263=$('#localVideoPreview');if(previewV263&&state.video._cameraStream)previewV263.srcObject=state.video._cameraStream;\n`,'simple-bind-head');

must(/const withTimeout=/,
`function launchSimpleSession(kind){const type='internal',name=state.user?.name||localStorage.displayName||'Host',language=localStorage.sourceLanguage||localStorage.language||locale[state.lang]||'ko-KR',title=kind==='video'?'Video Meeting':'Voice Meeting';state.sessionNotes=[];state._autoCapture=kind;state.meeting={id:null,type,title,status:'creating',participants:[],_peer:null,language,meeting_mode:kind==='video'?'video':'voice',_creating:true};state.view='room';state.captions=[];state.media.elapsed=0;state.media.recording=false;state.media.videoRecording=false;render();setTimeout(()=>{void syncSimpleSession({type,title,name,language,kind})},0)}\nasync function syncSimpleSession({type,title,name,language,kind}){const peer=\`peer_\${crypto.randomUUID().slice(0,8)}\`;if(state.meeting)state.meeting._peer=peer;try{const created=await withTimeout(api('/api/v1/meetings',{method:'POST',body:JSON.stringify({type,title,peer_id:peer,name,language,meeting_mode:kind==='video'?'video':'voice'})}),8000,'meeting_create_timeout');if(!state.meeting)return;state.meeting={...(created.data||{}),_peer:peer,language,meeting_mode:kind==='video'?'video':'voice',_creating:false};render();pollCaptions();pollSignals();if(kind==='video')await startVideoCapture();else await startRecording()}catch(e){if(state.meeting){state.meeting={...state.meeting,_creating:false,_createError:e.message};render()}}}\nasync function startVideoCapture(){if(state.media.videoRecording)return;try{const stream=await withTimeout(navigator.mediaDevices.getUserMedia({video:true,audio:{echoCancellation:true,noiseSuppression:true,autoGainControl:true}}),8000,'video_capture_timeout');state.video._cameraStream=stream;state.media.stream=stream;state.media.mic='ok';state.media.camera='ok';state.media.videoRecording=true;state.media.recording=true;state.media.startedAt=Date.now();state.media.elapsed=0;clearInterval(state.media.timer);state.media.timer=setInterval(()=>{state.media.elapsed=Math.floor((Date.now()-state.media.startedAt)/1000);const e=$('#recordTime');if(e)e.textContent=fmt(state.media.elapsed)},500);if(window.MediaRecorder){const chunks=[];const mr=new MediaRecorder(stream);state.media.videoRecorder=mr;mr.ondataavailable=e=>{if(e.data?.size)chunks.push(e.data)};mr.onstop=()=>{if(chunks.length){const blob=new Blob(chunks,{type:chunks[0]?.type||'video/webm'});state.media.lastVideoUrl=URL.createObjectURL(blob)}};mr.start(1000)}startSpeech();render()}catch(e){state.media.videoRecording=false;state.media.recording=false;state.media.mic='error';state.media.camera='error';render();alert('카메라/마이크를 사용할 수 없습니다: '+e.message)}}\nasync function saveSessionNote(){const el=$('#sessionNoteText'),text=el?.value?.trim();if(!text)return;state.sessionNotes=state.sessionNotes||[];state.sessionNotes.push({time:new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'}),text});if(el)el.value='';if(state.meeting?.id){try{await postCaption('[NOTE] '+text)}catch{}}render()}\nconst withTimeout=`,'simple-runtime');

must(/function stopRecording\(\)\{.*?\}\nfunction startSpeech/s,
`function stopRecording(){state.media.recording=false;state.media.videoRecording=false;clearInterval(state.media.timer);try{state.media.videoRecorder?.state!=='inactive'&&state.media.videoRecorder?.stop()}catch{}try{state._speech?.stop()}catch{}try{(state.media.stream?.getTracks?.()||[]).forEach(x=>x.stop());(state.video._cameraStream?.getTracks?.()||[]).forEach(x=>x.stop())}catch{}state.video._cameraStream=null;render()}\nfunction startSpeech`,'simple-stop');

await fs.writeFile(file,s,'utf8');
console.log('VoiceFlow simple capture-first UX applied');
